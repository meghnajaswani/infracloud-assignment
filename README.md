# infracloud-assignment
A RESTful URL Shortener built with Spring Boot that generates Base62 short codes, supports redirection, ensures idempotency, and exposes domain-based metrics using thread-safe in-memory storage.
